[package]
name = "subtext-container"
description = "Reading the subtitle tracks inside a film container for Subtext"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
subtext-core.workspace = true

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false, features = ["cargo_bench_support"] }
tempfile = "3.27.0"

[[bench]]
name = "probe"
harness = false

[[bench]]
name = "extract"
harness = false

[[bench]]
name = "cover"
harness = false

[lints]
workspace = true
